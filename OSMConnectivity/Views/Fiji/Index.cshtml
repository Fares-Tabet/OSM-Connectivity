@{
    ViewData["Title"] = "Fiji";
}
<!DOCTYPE html>
<html>
<head>
    <link rel="stylesheet" href="https://wagtrung.github.io/store/themify-icons/themify-icons.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">

    <title>Data Layer: Polygon</title>
    <meta name="viewport" content="initial-scale=1.0">
    <meta charset="utf-8">
    <style>
        /* Always set the map height explicitly to define the size of the div
        * element that contains the map. */
        body {
            overflow-y: scroll;
            font-family: sans-serif;
            background-color: #fefefe;
        }

        #map {
            height: 130%;
        }

        /* Optional: Makes the sample page fill the window. */
        html, body {
            height: 100%;
            margin: 0;
            padding: 0;
        }

        #legend {
            font-family: Arial, sans-serif;
            background: #fff;
            padding: 10px;
            margin: 10px;
            border: 3px solid #000;
            border-radius: 3px;
        }


        .category {
            position: absolute;
            width: 17em;
            margin-top: 60px;
            margin-left: 11px;
            height: 315px;
            top: 0;
            background: #fff;
            font-size: 1.6rem;
            border-radius: 5px;
        }

        .ctghead {
            padding: 2px 40px;
            border-bottom: 1px solid rgba(0, 0, 0, 0.09);
        }

            .ctghead h3 {
                display: inline-flex;
                align-items: center;
                font-size: 1.3rem;
                font-weight: 700;
            }

                .ctghead h3 i {
                    font-size: 2.5rem;
                    margin-right: 7px;
                }

        .ctgul {
            margin-top: 12px;
            padding: 0 6px;
        }

        .ctgulChild {
            margin: 0 0 0 30px;
            padding: 0 0 10px 0;
        }

        .ctgliChild {
            font-size: 1.2rem;
            display: block;
            position: relative;
            list-style: none;
            transition: .3s linear;
            box-shadow: transparent 0px 14px 13px -12px, transparent 0px 26px 32px -22px;
        }

        .ctgli:hover {
            border-left: 4px solid var(--hove);
            background: #d2ffe221;
            box-shadow: rgba(50, 50, 93, 0.08) 0px 14px 13px -12px, rgba(0, 0, 0, 0.16) 0px 26px 32px -22px;
        }

        .ctga, .ctgaChild {
            width: 100%;
            display: inline-flex;
            align-items: center;
            cursor: pointer;
            color: #333;
            text-decoration: none;
            padding: 13px 40px 13px 34px;
            position: relative;
            right: 0;
            transition: right linear .3s;
            border: 1px solid #ACB9BB;
            border-width: 1px;
            border-radius: 5px;
        }

            .ctga i {
                margin-right: 10px;
            }

        .ctgaChild {
            padding: 13px 13px 13px 36px;
        }

            .ctga:hover, .ctgaChild:hover {
                right: -4px;
                color: var(--hove);
                text-decoration: none;
            }

            ctga:active, ctgaChild:active, ctga:visited, ctgaChild:visited, ctga:focus, ctgaChild:focus {
                text-decoration: none;
            }

            .ctgaChild::before {
                top: 50%;
                transform: translateY(-50%);
                left: 20px;
                position: absolute;
                border-style: solid;
                border-color: transparent transparent transparent var(--hove);
                border-width: 4px;
                content: "";
            }

        .down {
            position: absolute;
            font-size: 1.3rem;
            right: 10%;
            top: 50%;
            transform: translateY(-50%) rotate(360deg);
        }

        .ctgulChild {
            display: none;
            text-decoration: none;
        }

        .ctgli {
            display: block;
            position: relative;
            list-style: none;
            border-left: 4px solid transparent;
            transition: .3s linear;
        }

        :root {
            --white: #fff;
            --black: #000;
            --textColor: #333;
            --hove: #04ca88;
        }

        label {
            font-weight: 100;
            font-size: smaller;
            margin-bottom: 0;
            text-decoration: none;
        }

        label:active, label:visited, labe:focus {
            text-decoration: none;
        }

        input[type="checkbox"] {
            vertical-align: middle;
        }

        input[type="radio"], input[type="checkbox"] {
            margin: -2px 0 0;
            line-height: normal;
        }

       
    </style>
</head>
<body>
        <div id="map"></div>

        <nav class="category" id="category">
            <div class="ctghead">
                <h3><i class="ti-list"></i>OSM Connectivity Tools</h3>
            </div>
            <ul class="ctgul">
                <li class="ctgli">
                    <a class="ctga">
                        <label for="incorrectConnectionsPR">
                            <input class="form-check-input move-left" type="checkbox" id="incorrectConnectionsPR" onclick="hideShowIncorrectConnectionsPR()">
                            Incorrect Connections PR
                        </label>
                    </a>
                </li>
                @*<li class="ctgli">
                    <a class="ctga">
                        <label for="incorrectConnectionsSEC">
                            <input class="form-check-input move-left" type="checkbox" id="incorrectConnectionsSEC" onclick="hideShowIncorrectConnectionsSEC()">
                            Incorrect Connections SEC
                        </label>
                    </a>
                </li>
                <li class="ctgli">
                    <a class="ctga">
                        <label for="incorrectConnectionsTER">
                            <input class="form-check-input move-left" type="checkbox" id="incorrectConnectionsTER" onclick="hideShowIncorrectConnectionsTER()">
                            Incorrect Connections TER
                        </label>
                    </a>
                </li>*@
                <li class="ctgli">
                    <div class="ctga" onclick="downloadFiles()">
                        <i class="ti-download"></i>
                        <label>
                            Download Fixes
                        </label>
                        <a href="~/Content/json_files/FJ/FJ_Fixes.zip" id="pr" download></a>
                        <script>
                        function downloadFiles() {
                            document.getElementById("pr").click();
                        }
                        </script>
                    </div>
                </li>
                <hr style="margin-bottom:0;margin-top:0;margin-left:0">
                <li class="ctgli">
                    <a class="ctga">
                        <label for="whitelistNodes">
                            <input class="form-check-input move-left" type="checkbox" id="whitelistNodes" onclick="hideShowWhitelistNodes()">
                            Whitelisted Nodes
                        </label>
                    </a>
                </li>
                <hr style="margin-bottom:0;margin-top:0;margin-left:0">
                <li class="ctgli">
                    <a href="./NewZealand" class="ctga">
                        <i class="ti-pin"></i>
                        <label>
                            Go to NZ
                        </label>
                    </a>
                </li>
                <hr style="margin-bottom:0;margin-top:0;margin-left:0">
                <li class="ctgli">
                    <a class="ctga" data-toggle="collapse" href="#FijiConnectivityTools" role="button" aria-expanded="false" aria-controls="FijiConnectivityTools" onclick="toggleSidebar()" style="text-decoration:none;color:#333;">
                        <i class="ti-pencil"></i>
                        <label>
                            FJ Connectivity Tools
                        </label>
                        <i class="ti-angle-down down"></i>
                    </a>

                    <div class="collapse" id="FijiConnectivityTools">
                        <ul>
                            <li class="ctgliChild">
                                <a class="ctgaChild">
                                    <label for="primary">
                                        <input class="form-check-input move-left" type="checkbox" id="primary" onclick="drawPrimary()">
                                        Primary Routes
                                    </label>
                                </a>
                            </li>
                            <li class="ctgliChild">
                                <a class="ctgaChild">
                                    <label for="secondary">
                                        <input class="form-check-input move-left" type="checkbox" id="secondary" onclick="drawSecondary()">
                                        Secondary Routes
                                    </label>
                                </a>
                            </li>
                            <li class="ctgliChild">
                                <a class="ctgaChild">
                                    <label for="tertiary">
                                        <input class="form-check-input move-left" type="checkbox" id="tertiary" onclick="drawTertiary()">
                                        Tertiary Routes
                                    </label>
                                </a>
                            </li>
                            <li class="ctgliChild">
                                <a class="ctgaChild">
                                    <label for="ferry">
                                        <input class="form-check-input move-left" type="checkbox" id="ferry" onclick="drawFerryRoutes()">
                                        Ferry Routes
                                    </label>
                                </a>
                            </li>
                            <hr style="margin-bottom:0;margin-top:0;margin-left:0">
                            <li class="ctgliChild">
                                <a class="ctgaChild">
                                    <label for="ferry_terminals">
                                        <input class="form-check-input move-left" type="checkbox" id="ferry_terminals" onclick="drawFerryTerminals()">
                                        Ferry Terminals
                                    </label>
                                </a>
                            </li>
                            <li class="ctgliChild">
                                <a class="ctgaChild">
                                    <label for="primary_disconnections">
                                        <input class="form-check-input move-left" type="checkbox" id="primary_disconnections" onclick="drawPrimaryDisconnections()">
                                        Primary Disconnections
                                    </label>
                                </a>
                            </li>
                            <li class="ctgliChild">
                                <a class="ctgaChild">
                                    <label for="secondary_disconnections">
                                        <input class="form-check-input move-left" type="checkbox" id="secondary_disconnections" onclick="drawSecondaryDisconnections()">
                                        Secondary Disconnections
                                    </label>
                                </a>
                            </li>
                            <li class="ctgliChild">
                                <a class="ctgaChild">
                                    <label for="primarViaSecondary">
                                        <input class="form-check-input move-left" type="checkbox" id="primarViaSecondary" onclick="demoPrimarViaSecondary()">
                                        Primary via Secondary
                                    </label>
                                </a>
                            </li>
                            <li class="ctgliChild">
                                <a class="ctgaChild">
                                    <label for="PS_via_ST">
                                        <input class="form-check-input move-left" type="checkbox" id="PS_via_ST" onclick="demoPSConnectivityViaST()">
                                        P/S Connectivity via S/T
                                    </label>
                                </a>
                            </li>
                            <li class="ctgliChild">
                                <a class="ctgaChild">
                                    <label for="PST_via_ST">
                                        <input class="form-check-input move-left" type="checkbox" id="PST_via_ST" onclick="demoPSTConnectivityViaST()">
                                        P/S/T Connectivity via S/T
                                    </label>
                                </a>
                            </li>
                        </ul>
                    </div>
                </li>
            </ul>
        </nav>

        <div id="legend">
            <h3 style="margin-top:auto;">Legend</h3>
            <img src="~/Content/images/F39C12.png" width="50" height="8"> Primary
            <br />
            <img src="~/Content/images/27AE60.png" width="50" height="8"> Secondary
            <br />
            <img src="~/Content/images/6f0909.png" width="50" height="8"> Tertiary
            <br />
            <img src="~/Content/images/darkBlue.png" width="50" height="8"> Ferry
            <br />
            <img src="~/Content/images/808b96.png" width="50" height="8"> Primary via Secondary
            <br />
            <img src="~/Content/images/6C3483.JPG" width="50" height="8"> P/S Connectivity via S/T
            <br />
            <img src="~/Content/images/096f09.png" width="50" height="8"> P/S/T Connectivity via S/T
            <br />
            <img src="~/Content/images/pin.png" width="45" height="50"> Incompatible Connection
            <br />
            <img src="~/Content/images/blue-pin.png" width="45" height="50"p> Whitelist Nodes
            <br />
            <img src="~/Content/images/ferry.png" width="45" height="50"> Ferry Terminals
        </div>

    <script src="https://maps.googleapis.com/maps/api/js?key=AIzaSyC2BmtjwtFmGF_Dh-J8ltarifOtyrRYd7w&callback=initMap"></script>
    <script src="~/Scripts/jquery-3.3.1.js"></script>
    <script type="text/javascript">

        // Set the sidebar to a larger height when expand the collapsed items.
        function toggleSidebar() {
            $('#FijiConnectivityTools').on('show.bs.collapse', function () {
                document.getElementById("category").style.height = "735px";
            });

            $('#FijiConnectivityTools').on('hide.bs.collapse', function () {
                document.getElementById("category").style.height = "315px";
            });
        }
        
        
        //Global variables:
        var incorrectMarkersDictionary = new Map();
        var incorrectMarkersDictionaryPR = new Map();
        var incorrectMarkersDictionarySEC = new Map();
        var incorrectMarkersDictionaryTER = new Map();
        var whitlelistNodes = new Map();
        var markerGroups = {
            incorrectMarkersDictionary,
            incorrectMarkersDictionaryPR,
            incorrectMarkersDictionarySEC,
            incorrectMarkersDictionaryTER,
            whitlelistNodes
        };

        var element = document.getElementById("map");

        var primaryLine = []
        var secondaryLine = []
        var tertiaryLine = []
        var ferryRoutesLine = []
        var ferryTerminalsMakers = []
        var primaryDisconnectionsLine = []
        var secondaryDisconnectionsLine = []
        var primarViaSecondaryLine = []
        var psConnectivityViaSTLine = [];
        var pstConnectivityViaSTLine = [];

        /*
        Build list of map types.
        You can also use var mapTypeIds = ["roadmap", "satellite", "hybrid", "terrain", "OSM"]
        but static lists suck when Google updates the default list of map types.
        */
        var mapTypeIds = [];
        for (var type in google.maps.MapTypeId) {
            mapTypeIds.push(google.maps.MapTypeId[type]);
        }
        mapTypeIds.push("OSM");

        var map = new google.maps.Map(element, {
            center: new google.maps.LatLng(-17.713371, 178.065033),
            zoom: 6,
            mapTypeId: "OSM",
            mapTypeControlOptions: {
                mapTypeIds: mapTypeIds
            }
        });

        map.mapTypes.set("OSM", new google.maps.ImageMapType({
            getTileUrl: function (coord, zoom) {
                // See above example if you need smooth wrapping at 180th meridian
                return "https://tile.openstreetmap.org/" + zoom + "/" + coord.x + "/" + coord.y + ".png";
            },
            tileSize: new google.maps.Size(256, 256),
            name: "OpenStreetMap",
            maxZoom: 18
        }));

        loadWhiteListMarkers();
        highlightIncorrectConnectionsPR(1);
        //highlightIncorrectConnectionsSEC(1);
        //highlightIncorrectConnectionsTER(1);

        function createInfoWindow(poly, content) {
            var infowindow = new google.maps.InfoWindow();
            google.maps.event.addListener(poly, 'click', function (event) {
                infowindow.setPosition(event.latLng);
                infowindow.setContent(content);
                infowindow.open(map);
            });

            google.maps.event.addListener(poly, 'mouseover', function (event) {
                infowindow.setPosition(event.latLng);
                infowindow.setContent(content);
                infowindow.open(map);
            });

            // Close the InfoWindow on mouseout:
            google.maps.event.addListener(poly, 'mouseout', function () {
                infowindow.close();
            });
        }

        // Draw the primary routes
        function drawPrimary() {
            @using Newtonsoft.Json;
            var json = @Html.Raw(JsonConvert.SerializeObject(ViewBag.primary));
            var count = Object.keys(json).length;
            var flightPath = null;

            for (var i = 0; i < count; i++) {

                var data = [];
                var ptsCount = Object.keys(json[i]['nodes']).length;

                for (var j = 0; j < ptsCount; j++) {
                    data.push({ lat: json[i]['nodes'][j]['Lat'], lng: json[i]['nodes'][j]['Lng'] });
                }
                var flightPath = new google.maps.Polyline({
                    path: data,
                    geodesic: true,
                    strokeColor: '#F39C12',
                    strokeOpacity: 1.0,
                    strokeWeight: 2
                });
                primaryLine.push(flightPath);
            }
            if (document.getElementById('primary').checked) {
                for (i = 0; i < primaryLine.length; i++) {
                    primaryLine[i].setMap(map);
                }
            } else {
                for (i = 0; i < primaryLine.length; i++) {
                    primaryLine[i].setMap(null);
                }
            }
        }

        // Draw secondary routes
        function drawSecondary() {
            @using Newtonsoft.Json;
            var json = @Html.Raw(JsonConvert.SerializeObject(ViewBag.secondary));
            var count = Object.keys(json).length;

            for (var i = 0; i < count; i++) {

                var data = [];
                var ptsCount = Object.keys(json[i]['nodes']).length;

                for (var j = 0; j < ptsCount; j++) {
                    data.push({ lat: json[i]['nodes'][j]['Lat'], lng: json[i]['nodes'][j]['Lng'] });
                }

                var flightPath = new google.maps.Polyline({
                    path: data,
                    geodesic: true,
                    strokeColor: '#27AE60',
                    strokeOpacity: 1.0,
                    strokeWeight: 2
                });
                secondaryLine.push(flightPath);
            }
            if (document.getElementById('secondary').checked) {
                for (i = 0; i < secondaryLine.length; i++) {
                    secondaryLine[i].setMap(map);
                }
            } else {
                for (i = 0; i < secondaryLine.length; i++) {
                    secondaryLine[i].setMap(null);
                }
            }
        }

        // Draw the tertiary routes
        function drawTertiary() {
            @using Newtonsoft.Json;
            var json = @Html.Raw(JsonConvert.SerializeObject(ViewBag.tertiary));
            var count = Object.keys(json).length;
            var flightPath = null;

            for (var i = 0; i < count; i++) {

                var data = [];
                var ptsCount = Object.keys(json[i]['nodes']).length;

                for (var j = 0; j < ptsCount; j++) {
                    data.push({ lat: json[i]['nodes'][j]['Lat'], lng: json[i]['nodes'][j]['Lng'] });
                }
                var flightPath = new google.maps.Polyline({
                    path: data,
                    geodesic: true,
                    strokeColor: '#6F0909',
                    strokeOpacity: 1.0,
                    strokeWeight: 2
                });

                tertiaryLine.push(flightPath);
            }
            if (document.getElementById('tertiary').checked) {
                for (i = 0; i < tertiaryLine.length; i++) {
                    tertiaryLine[i].setMap(map);
                }
            } else {
                for (i = 0; i < tertiaryLine.length; i++) {
                    tertiaryLine[i].setMap(null);
                }
            }
        }

        // Draw ferry routes
        function drawFerryRoutes() {
            @using Newtonsoft.Json;
            var json = @Html.Raw(JsonConvert.SerializeObject(ViewBag.ferryRoutes));
            var count = Object.keys(json).length;

            for (var i = 0; i < count; i++) {

                var data = [];
                var ptsCount = Object.keys(json[i]['nodes']).length;

                for (var j = 0; j < ptsCount; j++) {
                    data.push({ lat: json[i]['nodes'][j]['Lat'], lng: json[i]['nodes'][j]['Lng'] });
                }

                var flightPath = new google.maps.Polyline({
                    path: data,
                    geodesic: true,
                    strokeColor: '#00008B',
                    strokeOpacity: 1.0,
                    strokeWeight: 2
                });
                ferryRoutesLine.push(flightPath);
            }
            if (document.getElementById('ferry').checked) {
                for (i = 0; i < ferryRoutesLine.length; i++) {
                    ferryRoutesLine[i].setMap(map);
                }
            } else {
                for (i = 0; i < ferryRoutesLine.length; i++) {
                    ferryRoutesLine[i].setMap(null);
                }
            }
        }

        // Draw the ferry terminals
        function drawFerryTerminals() {
            @using Newtonsoft.Json;
            var json = @Html.Raw(JsonConvert.SerializeObject(ViewBag.ferryTerminals));
            var icon = {
                url: "http://maps.google.com/mapfiles/kml/shapes/ferry.png", // url
                scaledSize: new google.maps.Size(20, 20), // scaled size
            };
            var count = Object.keys(json).length;
            for (var i = 0; i < count; i++) {
                var marker = new google.maps.Marker({
                    map: map,
                    icon: icon,
                    animation: google.maps.Animation.DROP,
                    position: { lat: json[i]['Lat'], lng: json[i]['Lng'] },
                    title: json[i]['Id']
                });
                ferryTerminalsMakers.push(marker);
            }

            if (document.getElementById('ferry_terminals').checked) {
                for (var i = 0; i < ferryTerminalsMakers.length; i++) {
                    ferryTerminalsMakers[i].setMap(map);
                }
            } else {
                for (var i = 0; i < ferryTerminalsMakers.length; i++) {
                    this.ferryTerminalsMakers[i].setMap(null);
                }
                this.ferryTerminalsMakers = new Array();
            }
        }

        // Draw primary disconnections
        function drawPrimaryDisconnections() {

            @using Newtonsoft.Json;
            var json = @Html.Raw(JsonConvert.SerializeObject(ViewBag.disjointedSubTreeWaysPR));
            var count = Object.keys(json).length;


            for (var i = 0; i < count; i++) {
                var data = [];
                var ptsCount = Object.keys(json[i]['nodes']).length;


                for (var j = 0; j < ptsCount; j++)
                {
                    data.push({ lat: json[i]['nodes'][j]['Lat'], lng: json[i]['nodes'][j]['Lng'] });
                }

                var flightPath = new google.maps.Polyline({
                    path: data,
                    geodesic: true,
                    strokeColor: json[i]['colorCode'],
                    strokeOpacity: 1.0,
                    strokeWeight: 2
                });
                primaryDisconnectionsLine.push(flightPath);
            }
            if (document.getElementById('primary_disconnections').checked) {
                for (i = 0; i < primaryDisconnectionsLine.length; i++) {
                    primaryDisconnectionsLine[i].setMap(map);
                }
            } else {
                for (i = 0; i < primaryDisconnectionsLine.length; i++) {
                    primaryDisconnectionsLine[i].setMap(null);
                }
            }
        }

        // Draw disconnections secondary
        function drawSecondaryDisconnections() {
            @using Newtonsoft.Json;
            var json = @Html.Raw(JsonConvert.SerializeObject(ViewBag.disjointedSubTreeWaysSEC));
            var count = Object.keys(json).length;

            for (var i = 0; i < count; i++) {

                var data = [];
                var ptsCount = Object.keys(json[i]['nodes']).length;

                for (var j = 0; j < ptsCount; j++) {
                    data.push({ lat: json[i]['nodes'][j]['Lat'], lng: json[i]['nodes'][j]['Lng'] });
                }

                var flightPath = new google.maps.Polyline({
                    path: data,
                    geodesic: true,
                    strokeColor: json[i]['colorCode'],
                    strokeOpacity: 1.0,
                    strokeWeight: 2
                });
                secondaryDisconnectionsLine.push(flightPath);
            }
            if (document.getElementById('secondary_disconnections').checked) {
                for (i = 0; i < secondaryDisconnectionsLine.length; i++) {
                    secondaryDisconnectionsLine[i].setMap(map);
                }
            } else {
                for (i = 0; i < secondaryDisconnectionsLine.length; i++) {
                    secondaryDisconnectionsLine[i].setMap(null);
                }
            }
        }

        // Demo primary via secondary
        function demoPrimarViaSecondary() {
            @using Newtonsoft.Json;
            // @using System.Text.RegularExpressions;

            var json = @Html.Raw(JsonConvert.SerializeObject(ViewBag.FJ_P_viaS));
            var count = Object.keys(json).length;

            for (var i = 0; i < count; i++) {

                var data = [];
                var ptsCount = Object.keys(json[i]['nodes']).length;

                for (var j = 0; j < ptsCount; j++) {
                    data.push({ lat: json[i]['nodes'][j]['Lat'], lng: json[i]['nodes'][j]['Lng'] });
                }

                var flightPath = new google.maps.Polyline({
                    path: data,
                    geodesic: true,
                    strokeColor: '#808b96',
                    strokeOpacity: 1.0,
                    strokeWeight: 2
                });

                var wayid = json[i]['Id'].replace(/\D/g,'');
                // console.log(wayid);

                if (document.getElementById('primarViaSecondary').checked) {
                    var content = '<div id="content">' +
                        '<div id="siteNotice">' +
                        '</div>' +
                        '<h2 id="firstHeading" class="firstHeading">Primary via Secondary</h2>' +
                        '<div id="bodyContent">' +
                        '<p style="width:290px; font-weight: bold;"> This connectivity fix is made out of ' + count + ' ways.<br/><br/>' +
                        'Way id: ' + wayid + ' (' + json[i]['roadClass'] +')<br/>' +
                        '<a href="https://www.openstreetmap.org/edit?way=' + wayid + '" target="_blank" rel="noopener">' +
                        'Open in OSM iD <i class="fa fa-external-link"></i></a><br/>'+
                        '<a href="http://osmose.openstreetmap.fr/en/josm_proxy?load_object?objects=w' + wayid + '" target="_blank" rel="noopener">' +
                        'Open in JOSM iD <i class="fa fa-external-link"></i></a>' +
                        '</div></div> ';
                    createInfoWindow(flightPath, content);
                }
                primarViaSecondaryLine.push(flightPath);
            }
            if (document.getElementById('primarViaSecondary').checked) {
                for (i = 0; i < primarViaSecondaryLine.length; i++) {
                    primarViaSecondaryLine[i].setMap(map);
                }
            } else {
                for (i = 0; i < primarViaSecondaryLine.length; i++) {
                    primarViaSecondaryLine[i].setMap(null);
                }
            }
        }

        // Demo P/S connectivity via S/T
        function demoPSConnectivityViaST() {
            @using Newtonsoft.Json;
            var json = @Html.Raw(JsonConvert.SerializeObject(ViewBag.FJ_PS_viaST));
            var count = Object.keys(json).length;

            for (var i = 0; i < count; i++) {

                var data = [];
                var ptsCount = Object.keys(json[i]['nodes']).length;

                for (var j = 0; j < ptsCount; j++) {
                    data.push({ lat: json[i]['nodes'][j]['Lat'], lng: json[i]['nodes'][j]['Lng'] });
                }

                var flightPath = new google.maps.Polyline({
                    path: data,
                    geodesic: true,
                    strokeColor: '#6C3483',
                    strokeOpacity: 1.0,
                    strokeWeight: 2
                });

                var wayid = json[i]['Id'].replace(/\D/g,'');
                // console.log(wayid);

                if (document.getElementById('PS_via_ST').checked) {
                    var content = '<div id="content">' +
                        '<div id="siteNotice">' +
                        '</div>' +
                        '<h2 id="firstHeading" class="firstHeading">P/S Connectivity via S/T</h2>' +
                        '<div id="bodyContent">' +
                        '<p style="width:290px; font-weight: bold;"> This connectivity fix is made out of ' + count + ' ways.<br/><br/>' +
                        'Way id: ' + wayid + ' (' + json[i]['roadClass'] +')<br/>' +
                        '<a href="https://www.openstreetmap.org/edit?way=' + wayid + '" target="_blank" rel="noopener">' +
                        'Open in OSM iD <i class="fa fa-external-link"></i></a><br/>'+
                        '<a href="http://osmose.openstreetmap.fr/en/josm_proxy?load_object?objects=w' + wayid + '" target="_blank" rel="noopener">' +
                        'Open in JOSM iD <i class="fa fa-external-link"></i></a>' +
                        '</div></div> ';
                    createInfoWindow(flightPath, content);
                }
                psConnectivityViaSTLine.push(flightPath);
            }
            if (document.getElementById('PS_via_ST').checked) {
                for (i = 0; i < psConnectivityViaSTLine.length; i++) {
                    psConnectivityViaSTLine[i].setMap(map);
                }
            } else {
                for (i = 0; i < psConnectivityViaSTLine.length; i++) {
                    psConnectivityViaSTLine[i].setMap(null);
                }
            }
        }

        // Demo P/ST connectivity via S/T
        function demoPSTConnectivityViaST() {
            @using Newtonsoft.Json;
            var json = @Html.Raw(JsonConvert.SerializeObject(ViewBag.FJ_PST_viaST));
            var count = Object.keys(json).length;

            for (var i = 0; i < count; i++) {

                var data = [];
                var ptsCount = Object.keys(json[i]['nodes']).length;

                for (var j = 0; j < ptsCount; j++) {
                    data.push({ lat: json[i]['nodes'][j]['Lat'], lng: json[i]['nodes'][j]['Lng'] });
                }

                var flightPath = new google.maps.Polyline({
                    path: data,
                    geodesic: true,
                    strokeColor: '#096F09',
                    strokeOpacity: 1.0,
                    strokeWeight: 2
                });

                var wayid = json[i]['Id'].replace(/\D/g,'');
                // console.log(wayid);

                if (document.getElementById('PST_via_ST').checked) {
                    var content = '<div id="content">' +
                        '<div id="siteNotice">' +
                        '</div>' +
                        '<h2 id="firstHeading" class="firstHeading">P/S/T Connectivity via S/T</h2>' +
                        '<div id="bodyContent">' +
                        '<p style="width:290px; font-weight: bold;"> This connectivity fix is made out of ' + count + ' ways.<br/><br/>' +
                        'Way id: ' + wayid + ' (' + json[i]['roadClass'] +')<br/>' +
                        '<a href="https://www.openstreetmap.org/edit?way=' + wayid + '" target="_blank" rel="noopener">' +
                        'Open in OSM iD <i class="fa fa-external-link"></i></a><br/>'+
                        '<a href="http://osmose.openstreetmap.fr/en/josm_proxy?load_object?objects=w' + wayid + '" target="_blank" rel="noopener">' +
                        'Open in JOSM iD <i class="fa fa-external-link"></i></a>' +
                        '</div></div> ';
                    createInfoWindow(flightPath, content);
                }
                pstConnectivityViaSTLine.push(flightPath);
            }
            if (document.getElementById('PST_via_ST').checked) {
                for (i = 0; i < pstConnectivityViaSTLine.length; i++) {
                    pstConnectivityViaSTLine[i].setMap(map);
                }
            } else {
                for (i = 0; i < pstConnectivityViaSTLine.length; i++) {
                    pstConnectivityViaSTLine[i].setMap(null);
                }
            }
        }

        var log = ''
        function download() {

            @using Newtonsoft.Json;
            var json = @Html.Raw(JsonConvert.SerializeObject(ViewBag.demoTrunkTrunkLinkConnectivity));
            var filename = "NZ_TrunkTrunkLink_MotorwayConnectivityFix.json";
            var dataStr = "data:text/json;charset=utf-8," + encodeURIComponent(JSON.stringify(json));
            //creating an invisible element
            var element = document.createElement('a');
            element.setAttribute('href', dataStr);

            element.setAttribute('download', filename);

            document.body.appendChild(element);

            //onClick property
            element.click();

            document.body.removeChild(element);
            @using Newtonsoft.Json   
            var json = @Html.Raw(JsonConvert.SerializeObject(ViewBag.demoTrunkTrunkLinkPrimaryConnectivity));
            var filename = "NZ_TrunkTrunkLinkPrimary_MotorwayConnectivityFix.json";
            var dataStr = "data:text/json;charset=utf-8," + encodeURIComponent(JSON.stringify(json));
            //creating an invisible element
            var element = document.createElement('a');
            element.setAttribute('href', dataStr);

            element.setAttribute('download', filename);

            document.body.appendChild(element);

            //onClick property
            element.click();

            document.body.removeChild(element);
        }

        function getData(ajaxurl) {
            return $.ajax({
                url: ajaxurl,
                method: "GET",
                timeout: 20000
            });
        };

        async function highlightIncorrectConnectionsPR(flag) {
            var json = await getData("Fiji/getIncorrectConnectionsDataPR")
            json = JSON.parse(json)

            var count = Object.keys(json).length;

            for (var i = 0; i < count; i++) {

                // Add a marker pin of each disconnection on the map
                if (flag == 1) {
                    var marker = new google.maps.Marker({
                        map: map,
                        //icon: 'http://maps.google.com/mapfiles/ms/icons/blue-dot.png',
                        animation: google.maps.Animation.DROP,

                        position: { lat: json[i]['Lat'], lng: json[i]['Lng'] },
                        clickable: true,
                        title: json[i]['Id']
                    });
                    marker.setVisible(false);

                }

                var disconnectionInfo = '';
                var NodeWayCount = json[i]['roads'].length;


                var motorwayString = '';
                var disconnectedWaysString = '';

                for (var j = 0; j < NodeWayCount; j++)
                {

                    if (json[i]['roads'][j]['roadClass'] != 'motorway' && json[i]['roads'][j]['roadClass'] != 'motorway_link')
                    {
                        disconnectedWaysString += json[i]['roads'][j]['roadClass'] + ' (' + json[i]['roads'][j]['Id'] + '), ';
                    }
                    else
                    {
                        motorwayString += json[i]['roads'][j]['Id'] + ', ';
                    }
                }

                motorwayString = motorwayString.slice(0, -2);

                if (NodeWayCount == 1) {
                    disconnectionInfo = '<div id="content">' +
                        '<div id="siteNotice">' +
                        '</div>' +
                        '<h2 id="firstHeading" class="firstHeading">Incorrect Connection</h2>' +
                        '<div id="bodyContent">' +
                        '<p style="width:290px"><a href="https://www.openstreetmap.org/node/' + json[i]['Id'] +
                        '" target="_blank" rel="noopener"><b>Node ' + json[i]['Id'] + '</a> is an incorrect connection in <a href="https://www.openstreetmap.org/way/' +
                        motorwayString +
                        '" target="_blank" rel="noopener"> ' + motorwayString +'</a><br/><br/>'
                        + '<a href="https://www.openstreetmap.org/edit?node=' + json[i]['Id'] + '" target="_blank" rel="noopener">' +
                        'Open in OSM iD <i class="fa fa-external-link" ></i> </a><br/><br/>'+
                        '<a href="http://osmose.openstreetmap.fr/en/josm_proxy?load_object?objects=n' + json[i]['Id'] + '" target="_blank" rel="noopener">'+
                        '<i class="fa fa-external-link" ></i> Open in JOSM iD </a><br/><br/>'+
                        +'<button onclick = addToWhitelist('+json[i]['Id']+')> Whitelist </button>'+
                        '</div></div> ';

                    log = log +"Node " + json[i]['Id'] + "\nNode " + json[i]['Id'] + ' is a disconnection in the motorway: ' + motorwayString +
                        '\nFor more information and options go to: ' + 'https://www.openstreetmap.org/node/' + json[i]['Id'] + '\n\n\n';


                }// if there are 2 ways corresponding to the node, then one is motorway and the other is the disconnection
                else

                {
                    disconnectedWaysString = disconnectedWaysString.slice(0, -2);
                    disconnectedWaysString += motorwayString == "" ? "" : ", motorway (" + motorwayString + ")";

                    disconnectionInfo = '<div id="content">' +
                        '<div id="siteNotice">' +
                        '</div>' +
                        '<h2 id="firstHeading" class="firstHeading">Incorrect Connection</h2>' +
                        '<div id="bodyContent">' +
                        '<p style="width:290px"><a href="https://www.openstreetmap.org/node/' + json[i]['Id'] +
                        '" target="_blank" rel="noopener"><b>Node ' + json[i]['Id'] + '</a> is an incorrect connection between:' +
                        '<br/>' + disconnectedWaysString + '<br/><br/>' +
                        '<a href="https://www.openstreetmap.org/edit?node=' + json[i]['Id'] + '" target="_blank" rel="noopener">' +
                        'Open in OSM iD <i class="fa fa-external-link" ></i></a><br/>'+
                        '<a href="http://osmose.openstreetmap.fr/en/josm_proxy?load_object?objects=n' + json[i]['Id'] + '" target="_blank" rel="noopener">' +
                        ' Open in JOSM iD <i class="fa fa-external-link" ></i></a><br/><br/><button onclick = addToWhitelist('+json[i]['Id']+')> Whitelist </button>' +
                        '</div></div> ';

                    log = log + "Node " + json[i]['Id']+ "\nNode " + json[i]['Id'] + ' is a disconnection in the motorway: ' + motorwayString +
                        '\nand Ways: ' + disconnectedWaysString +
                        '\nFor more information and options go to: ' + 'https://www.openstreetmap.org/node/' + json[i]['Id'] + '\n\n\n';
                }

                if (flag == 1) {
                    marker.info = new google.maps.InfoWindow({
                        content: disconnectionInfo
                    });

                    //adding the onclick listener event for each marker
                    google.maps.event.addListener(marker, 'click', function () {
                        var marker_map = this.getMap();
                        this.info.open(marker_map, this);
                    });
                }

                //add the markers to global variable incorrectMarkersDictionaryPR
                markerGroups['incorrectMarkersDictionaryPR'].set(json[i]['Id'],marker);
            }
        }

        async function highlightIncorrectConnectionsSEC(flag) {
            var json = await getData("Fiji/getIncorrectConnectionsDataSEC")
            json = JSON.parse(json)

            var count = Object.keys(json).length;

            for (var i = 0; i < count; i++) {

                // Add a marker pin of each disconnection on the map
                if (flag == 1) {
                    var marker = new google.maps.Marker({
                        map: map,
                        //icon: 'http://maps.google.com/mapfiles/ms/icons/blue-dot.png',
                        animation: google.maps.Animation.DROP,

                        position: { lat: json[i]['Lat'], lng: json[i]['Lng'] },
                        clickable: true,
                        title: json[i]['Id']
                    });
                    marker.setVisible(false);

                }

                var disconnectionInfo = '';
                var NodeWayCount = json[i]['roads'].length;


                var motorwayString = '';
                var disconnectedWaysString = '';

                for (var j = 0; j < NodeWayCount; j++)
                {

                    if (json[i]['roads'][j]['roadClass'] != 'motorway' && json[i]['roads'][j]['roadClass'] != 'motorway_link')
                    {
                        disconnectedWaysString += json[i]['roads'][j]['roadClass'] + ' (' + json[i]['roads'][j]['Id'] + '), ';
                    }
                    else
                    {
                        motorwayString += json[i]['roads'][j]['Id'] + ', ';
                    }
                }

                motorwayString = motorwayString.slice(0, -2);

                if (NodeWayCount == 1) {
                    disconnectionInfo = '<div id="content">' +
                        '<div id="siteNotice">' +
                        '</div>' +
                        '<h2 id="firstHeading" class="firstHeading">Incorrect Connection</h2>' +
                        '<div id="bodyContent">' +
                        '<p style="width:250px"><a href="https://www.openstreetmap.org/node/' + json[i]['Id'] +
                        '" target="_blank" rel="noopener"><b>Node ' + json[i]['Id'] + '</a> connects a <a href="https://www.openstreetmap.org/way/' +
                        motorwayString +
                        '" target="_blank" rel="noopener">motorway ( ' + motorwayString +')</a> with another incompatible road type<br/><br/>' +

                        '<a href="https://www.openstreetmap.org/edit?node=' + json[i]['Id'] + '" target="_blank" rel="noopener">' +
                        'Open in OSM iD </a><br/><br/><button onclick = addToWhitelist('+json[i]['Id']+')> Whitelist </button>'+
                        '</div></div> ';

                    log = log +"Node " + json[i]['Id'] + "\nNode " + json[i]['Id'] + ' is a disconnection in the motorway: ' + motorwayString +
                        '\nFor more information and options go to: ' + 'https://www.openstreetmap.org/node/' + json[i]['Id'] + '\n\n\n';


                }// if there are 2 ways corresponding to the node, then one is motorway and the other is the disconnection
                else

                {
                    disconnectedWaysString = disconnectedWaysString.slice(0, -2);
                    disconnectedWaysString += motorwayString == "" ? "" : ", motorway (" + motorwayString + ")";

                    disconnectionInfo = '<div id="content">' +
                        '<div id="siteNotice">' +
                        '</div>' +
                        '<h2 id="firstHeading" class="firstHeading">Incorrect Connection</h2>' +
                        '<div id="bodyContent">' +
                        '<p style="width:250px"><a href="https://www.openstreetmap.org/node/' + json[i]['Id'] +
                        '" target="_blank" rel="noopener"><b>Node ' + json[i]['Id'] + '</a> is an incorrect connection between:' +
                        '<br/>' + disconnectedWaysString + '<br/><br/>' +

                        '<a href="https://www.openstreetmap.org/edit?node=' + json[i]['Id'] + '" target="_blank" rel="noopener">' +
                        'Open in OSM iD </a><br/><br/><button onclick = addToWhitelist('+json[i]['Id']+')> Whitelist </button>' +
                        '</div></div> ';

                    log = log + "Node " + json[i]['Id']+ "\nNode " + json[i]['Id'] + ' is a disconnection in the motorway: ' + motorwayString +
                        '\nand Ways: ' + disconnectedWaysString +
                        '\nFor more information and options go to: ' + 'https://www.openstreetmap.org/node/' + json[i]['Id'] + '\n\n\n';
                }

                if (flag == 1) {
                    marker.info = new google.maps.InfoWindow({
                        content: disconnectionInfo
                    });

                    //adding the onclick listener event for each marker
                    google.maps.event.addListener(marker, 'click', function () {
                        var marker_map = this.getMap();
                        this.info.open(marker_map, this);
                    });
                }

                //add the markers to global variable incorrectMarkersDictionarySEC
                markerGroups['incorrectMarkersDictionarySEC'].set(json[i]['Id'],marker);
            }
        }

        async function highlightIncorrectConnectionsTER(flag) {
            var json = await getData("Fiji/getIncorrectConnectionsDataTER")
            json = JSON.parse(json)

            var count = Object.keys(json).length;

            for (var i = 0; i < count; i++) {

                // Add a marker pin of each disconnection on the map
                if (flag == 1) {
                    var marker = new google.maps.Marker({
                        map: map,
                        //icon: 'http://maps.google.com/mapfiles/ms/icons/blue-dot.png',
                        animation: google.maps.Animation.DROP,

                        position: { lat: json[i]['Lat'], lng: json[i]['Lng'] },
                        clickable: true,
                        title: json[i]['Id']
                    });
                    marker.setVisible(false);

                }

                var disconnectionInfo = '';
                var NodeWayCount = json[i]['roads'].length;


                var motorwayString = '';
                var disconnectedWaysString = '';

                for (var j = 0; j < NodeWayCount; j++)
                {

                    if (json[i]['roads'][j]['roadClass'] != 'motorway' && json[i]['roads'][j]['roadClass'] != 'motorway_link')
                    {
                        disconnectedWaysString += json[i]['roads'][j]['roadClass'] + ' (' + json[i]['roads'][j]['Id'] + '), ';
                    }
                    else
                    {
                        motorwayString += json[i]['roads'][j]['Id'] + ', ';
                    }
                }

                motorwayString = motorwayString.slice(0, -2);

                if (NodeWayCount == 1) {
                    disconnectionInfo = '<div id="content">' +
                        '<div id="siteNotice">' +
                        '</div>' +
                        '<h2 id="firstHeading" class="firstHeading">Incorrect Connection</h2>' +
                        '<div id="bodyContent">' +
                        '<p style="width:250px"><a href="https://www.openstreetmap.org/node/' + json[i]['Id'] +
                        '" target="_blank" rel="noopener"><b>Node ' + json[i]['Id'] + '</a> connects a <a href="https://www.openstreetmap.org/way/' +
                        motorwayString +
                        '" target="_blank" rel="noopener">motorway ( ' + motorwayString +')</a> with another incompatible road type<br/><br/>' +

                        '<a href="https://www.openstreetmap.org/edit?node=' + json[i]['Id'] + '" target="_blank" rel="noopener">' +
                        'Open in OSM iD </a><br/><br/><button onclick = addToWhitelist('+json[i]['Id']+')> Whitelist </button>'+
                        '</div></div> ';

                    log = log +"Node " + json[i]['Id'] + "\nNode " + json[i]['Id'] + ' is a disconnection in the motorway: ' + motorwayString +
                        '\nFor more information and options go to: ' + 'https://www.openstreetmap.org/node/' + json[i]['Id'] + '\n\n\n';


                }// if there are 2 ways corresponding to the node, then one is motorway and the other is the disconnection
                else

                {
                    disconnectedWaysString = disconnectedWaysString.slice(0, -2);
                    disconnectedWaysString += motorwayString == "" ? "" : ", motorway (" + motorwayString + ")";

                    disconnectionInfo = '<div id="content">' +
                        '<div id="siteNotice">' +
                        '</div>' +
                        '<h2 id="firstHeading" class="firstHeading">Incorrect Connection</h2>' +
                        '<div id="bodyContent">' +
                        '<p style="width:250px"><a href="https://www.openstreetmap.org/node/' + json[i]['Id'] +
                        '" target="_blank" rel="noopener"><b>Node ' + json[i]['Id'] + '</a> is an incorrect connection between:' +
                        '<br/>' + disconnectedWaysString + '<br/><br/>' +

                        '<a href="https://www.openstreetmap.org/edit?node=' + json[i]['Id'] + '" target="_blank" rel="noopener">' +
                        'Open in OSM iD </a><br/><br/><button onclick = addToWhitelist('+json[i]['Id']+')> Whitelist </button>' +
                        '</div></div> ';

                    log = log + "Node " + json[i]['Id']+ "\nNode " + json[i]['Id'] + ' is a disconnection in the motorway: ' + motorwayString +
                        '\nand Ways: ' + disconnectedWaysString +
                        '\nFor more information and options go to: ' + 'https://www.openstreetmap.org/node/' + json[i]['Id'] + '\n\n\n';
                }

                if (flag == 1) {
                    marker.info = new google.maps.InfoWindow({
                        content: disconnectionInfo
                    });

                    //adding the onclick listener event for each marker
                    google.maps.event.addListener(marker, 'click', function () {
                        var marker_map = this.getMap();
                        this.info.open(marker_map, this);
                    });
                }

                //add the markers to global variable incorrectMarkersDictionaryTER
                markerGroups['incorrectMarkersDictionaryTER'].set(json[i]['Id'],marker);
            }
        }

        function hideShowIncorrectConnectionsPR()
        {

            if (markerGroups['incorrectMarkersDictionaryPR'].size > 0)
            {
                if (markerGroups['incorrectMarkersDictionaryPR'].values().next().value.getVisible() == true) {
                    for (const [key, value] of markerGroups['incorrectMarkersDictionaryPR'].entries()) {
                        value.setVisible(false);
                    }
                } else
                {
                    for (const [key, value] of markerGroups['incorrectMarkersDictionaryPR'].entries()) {
                        value.setVisible(true);
                    }
                }
            }
        }

        function hideShowIncorrectConnectionsSEC()
        {

            if (markerGroups['incorrectMarkersDictionarySEC'].size > 0)
            {
                if (markerGroups['incorrectMarkersDictionarySEC'].values().next().value.getVisible() == true) {
                    for (const [key, value] of markerGroups['incorrectMarkersDictionarySEC'].entries()) {
                        value.setVisible(false);
                    }
                } else
                {
                    for (const [key, value] of markerGroups['incorrectMarkersDictionarySEC'].entries()) {
                        value.setVisible(true);
                    }
                }
            }
        }

        function hideShowIncorrectConnectionsTER()
        {

            if (markerGroups['incorrectMarkersDictionaryTER'].size > 0)
            {
                if (markerGroups['incorrectMarkersDictionaryTER'].values().next().value.getVisible() == true) {
                    for (const [key, value] of markerGroups['incorrectMarkersDictionaryTER'].entries()) {
                        value.setVisible(false);
                    }
                } else
                {
                    for (const [key, value] of markerGroups['incorrectMarkersDictionaryTER'].entries()) {
                        value.setVisible(true);
                    }
                }
            }
        }

        function loadWhiteListMarkers() {

            var json;

            $.ajax({
                url: "Fiji/getWhiteListData",
                method: "GET",
                success: function (data) {
                    data = JSON.parse(data);
                    json = data;

                },
                error: function (err) {
                    console.log(err)
                },
                async:false

            });

            var count = Object.keys(json).length;

            for (var i = 0; i < count; i++) {

                // Add a marker pin of each disconnection on the map
                var marker = new google.maps.Marker({
                    map: map,
                    icon: 'http://maps.google.com/mapfiles/ms/icons/blue-dot.png',
                    animation: google.maps.Animation.DROP,

                    position: { lat: json[i]['Lat'], lng: json[i]['Lng'] },
                    clickable: true,
                    title: json[i]['Id']

                });

                marker.setVisible(false);

                var NodeWayCount = json[i]['roads'].length;

                var motorwayString = '';
                var disconnectedWaysString = '';

                for (var j = 0; j < NodeWayCount; j++) {

                    if (json[i]['roads'][j]['roadClass'] != 'motorway' && json[i]['roads'][j]['roadClass'] != 'motorway_link') {
                        disconnectedWaysString += json[i]['roads'][j]['roadClass'] + ' (' + json[i]['roads'][j]['Id'] + '), ';
                    }
                    else {
                        motorwayString += json[i]['roads'][j]['Id'] + ', ';
                    }
                }

                motorwayString = motorwayString.slice(0, -2);

                if (NodeWayCount == 1) {
                    disconnectionInfo = '<div id="content">' +
                        '<div id="siteNotice">' +
                        '</div>' +
                        '<h2 id="firstHeading" class="firstHeading">Incorrect Connection</h2>' +
                        '<div id="bodyContent">' +
                        '<p style="width:290px"><a href="https://www.openstreetmap.org/node/' + json[i]['Id'] +
                        '" target="_blank" rel="noopener"><b>Node ' + json[i]['Id'] + '</a> is an incorrect connection in <a href="https://www.openstreetmap.org/way/' +
                        motorwayString +
                        '" target="_blank" rel="noopener"> ' + motorwayString + '</a> <br/><br/>' +
                        '<br /> <br /> <button onclick= removeFromWhitelist('+json[i]['Id']+')> Remove from Whitelist </button > '+
                        '</div></div> ';



                }// if there are 2 ways corresponding to the node, then one is motorway and the other is the disconnection
                else {
                    disconnectedWaysString = disconnectedWaysString.slice(0, -2);

                    disconnectionInfo = '<div id="content">' +
                        '<div id="siteNotice">' +
                        '</div>' +
                        '<h2 id="firstHeading" class="firstHeading">Incorrect Connection</h2>' +
                        '<div id="bodyContent">' +
                        '<p style="width:290px"><a href="https://www.openstreetmap.org/node/' + json[i]['Id'] +
                        '" target="_blank" rel="noopener"><b>Node ' + json[i]['Id'] + '</a> is an incorrect connection between' +
                        '<br/>' + disconnectedWaysString + '<br/><br/>' +
                        '<br /> <br /> <button onclick= removeFromWhitelist(' + json[i]['Id'] + ')> Remove from Whitelist </button > '+
                        '</div></div> ';

                }
                marker.info = new google.maps.InfoWindow({
                    content: disconnectionInfo
                });

                //adding the onclick listener event for each marker
                google.maps.event.addListener(marker, 'click', function () {
                    var marker_map = this.getMap();
                    this.info.open(marker_map, this);
                });

                //add the markers to global variable incorrectMarkersDictionary
                markerGroups['whitlelistNodes'].set(json[i]['Id'], marker);
            }
        }

        //when a user clicks whitelist node
        function addToWhitelist(id)
        {
            //call controller function
            $.get("/Fiji/addToWhitelist",{id:id}, function (data) {
                alert(data);
            });

           //create whitelist node
            var whitelistMarker = new google.maps.Marker({
                        map: map,
                        icon: 'http://maps.google.com/mapfiles/ms/icons/blue-dot.png',
                        position: markerGroups['incorrectMarkersDictionaryPR'].get(''+id).position ,
                clickable: true,
                title:''+id

            });
            //set visibility to false
            whitelistMarker.setVisible(false);
            //remove the node from all the layers and add it to whitelist layer
            markerGroups['incorrectMarkersDictionaryPR'].get('' + id).setMap(null);
            markerGroups['incorrectMarkersDictionaryPR'].delete('' + id);
            markerGroups['whitlelistNodes'].set('' + id, whitelistMarker);

        }

        //when a user wants to remove a node from the whitelist
        function removeFromWhitelist(id) {
            //call controller function
            $.get("/Fiji/removeFromWhitelist", { id: id }, function (data) {
                alert(data);
            });

            //create whitelist node
            var incorrectConnectionNode = new google.maps.Marker({
                map: map,
                //icon: 'http://maps.google.com/mapfiles/ms/icons/blue-dot.png',
                position: markerGroups['whitlelistNodes'].get('' + id).position,
                clickable: true,
                title: '' + id

            });

            //set visibility to false
            incorrectConnectionNode.setVisible(false);
            //remove the node from all the layers and add it to whitelist layer
            markerGroups['whitlelistNodes'].get('' + id).setMap(null);
            markerGroups['whitlelistNodes'].delete('' + id);
            markerGroups['incorrectMarkersDictionaryPR'].set('' + id, incorrectConnectionNode);

        }

        function hideShowWhitelistNodes()
        {
            if (markerGroups['whitlelistNodes'].size > 0)
            {
                if (markerGroups['whitlelistNodes'].values().next().value.getVisible() == true) {
                    for (const [key, value] of markerGroups['whitlelistNodes'].entries()) {
                        value.setVisible(false);
                    }
                } else
                {
                    for (const [key, value] of markerGroups['whitlelistNodes'].entries()) {
                        value.setVisible(true);
                    }
                }
            }
        }
        map.controls[google.maps.ControlPosition.RIGHT_BOTTOM].push(legend);
    </script>
</body>
</html>
